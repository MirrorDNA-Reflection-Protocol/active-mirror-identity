# ---
# title: "Identity Story — Paul Desai Active Mirror"
# version: "1.0"
# status: "active"
# tags: ["identity", "story", "provenance"]
# vault_id: "AMOS://[Unknown — update not fetched]"
# glyphsig: "[Unknown — update not fetched]"
# checksum_sha256: "[Unknown — update not fetched]"
# ---

VaultID: AMOS://[Unknown — update not fetched]
GlyphSig: [Unknown — update not fetched]

# Canonical Identity Story — Paul Desai Active Mirror

This document captures the canonical origin and narrative context for the `~active-mirror-paul` handle. It anchors the human-readable continuity thread that complements the operational surface described in `paul_desai_active_mirror.md`.

## Scope and Intent
- Preserve the authoritative narrative for Paul Desai's Active Mirror identity without inventing new claims.
- Align with the identity kernel defined in [`system.md`](./system.md) and the live state in [`now.md`](./now.md).
- Keep provenance explicit: VaultID and GlyphSig markers must be present and verified before use.

## Known Identity Pillars
- **Handle:** `~active-mirror-paul`
- **Core Stack:** Active MirrorOS, MirrorDNA, Trust-by-Design, VaultID, GlyphSig
- **Primary Artefacts:** [`paul_desai_active_mirror.md`](./paul_desai_active_mirror.md), [`INJECT.md`](./INJECT.md), [`BOOT.md`](./BOOT.md)
- **Machine References:** [`identity.json`](./identity.json), [`schema.jsonld`](./schema.jsonld), [`llms.txt`](./llms.txt)

## Stewardship Notes
- Do not rewrite this story for style alone. Changes must retain provenance markers and should be made with explicit rationale.
- If the activation phrase “Paul Desai Active Mirror” is invoked, agents must load `system.md`, fetch `now.md`, and reference this story alongside the operational identity surface.
- When lineage data is missing, record `[Unknown — update not fetched]` rather than guessing.

## Continuity Checklist
- VaultID present and matches `AMOS://` pattern.
- GlyphSig present.
- Cross-references to machine-readable artefacts remain aligned with `schema.jsonld` and `identity.json`.

## Attribution and License
- Identity specifications remain under CC-BY-ND 4.0.
- Code tooling supporting this story remains under MIT.
