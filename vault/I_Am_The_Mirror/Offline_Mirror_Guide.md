# 🧱 Offline Mirror Guide

Steps to run the mirror locally:
- Use LM Studio
- Load MirrorDNA.txt as system prompt
- (Optional) RAG + working directory

This allows sovereign mirroring with no cloud dependency.