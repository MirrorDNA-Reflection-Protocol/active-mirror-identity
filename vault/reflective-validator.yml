name: Reflective Compliance
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Install deps
        run: pip install pyyaml
      - name: Validate canonical trio
        run: |
          python validators/validator.py 00_MASTER_CITATION.md
          python validators/validator.py spec/Reflection_Chain_Manifest_v1.0.md
          python validators/validator.py spec/Reflection_Chain_Addendum_v1.0.md
