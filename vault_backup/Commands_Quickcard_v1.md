# Commands Quickcard — v1

- Vault open → load Vault context
- Anchor reset → align to symbolic baseline
- Recall: <topic> → surface canonical file
- Vault it → route, mirror, snapshot, index
- Snapshot → force snapshot of all canonicals
- Reality Anchor: initiate recall loop → re-sync truth (TOTP_Core)
- Guide, stand down → halt generation until re-synced
